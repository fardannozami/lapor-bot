Code is idiomatic Go with small focused use cases and repository-backed persistence. Prefer minimal targeted changes over broad refactors. User-facing bot copy is typically Indonesian with casual motivational tone. Domain helpers for date/week logic live in internal/domain/report.go. Business logic goes in internal/app/usecase, while SQLite queries stay in internal/infra/sqlite. Tests are standard Go tests using in-memory or mock repositories; add focused regression tests for routing or date-boundary behavior when needed.